# dbt Modeling Patterns

Status: skeleton.

Purpose: document `xf.dbt` usage for inline models and existing dbt projects.

Expected coverage:

- Inline model mode.
- `projectDir` mode.
- Referencing upstream DuckDB tables.
- `outputModel` behavior.
- dbt profile generation.
- Where dbt fits in local studio workflows.

Relevant node docs:

- `docs/01_nodes/02_transform-node-contracts.md`
- `docs/01_nodes/05_control-and-code-node-contracts.md`

TODO:

- Add inline dbt example.
- Add existing dbt project example.
- Document dbt setup/runtime checks.
