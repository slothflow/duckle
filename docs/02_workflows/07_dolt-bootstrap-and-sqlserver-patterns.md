# Dolt Bootstrap and SQL Server Patterns

Status: skeleton.

Purpose: document how to bring Dolt/local SQL workflow ideas from Stitchly v1 into v2.

Expected coverage:

- `code.shell` for Dolt repo/bootstrap CLI steps.
- Dolt SQL Server through `src.mysql`.
- Local Dolt workspace layout.
- Repeatable setup/check/assert patterns.
- Migration from v1 Dolt bootstrap scripts.

Relevant node docs:

- `docs/01_nodes/01_source-node-contracts.md`
- `docs/01_nodes/05_control-and-code-node-contracts.md`

TODO:

- Recover or paste v1 Dolt bootstrap commands.
- Test Dolt SQL server with `src.mysql`.
- Add bootstrap workflow recipe.
